import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.studio9.tomatotime',
  appName: 'Tomato Time',
  webDir: 'dist',
  android: {
    backgroundColor: '#fff5f0',
  },
}

export default config
